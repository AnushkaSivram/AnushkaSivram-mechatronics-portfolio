# Adaptive Robotic Gripper

Models a two-finger parallel gripper closing on a cylindrical part. It sweeps opening width and reports contact normal force, friction-limited payload, motor torque, and a conservative finger bending stress. The linkage is a simple force-ratio approximation for early actuator sizing.

Run `python3 simulate.py`. Review `gripper_sweep.csv` in a spreadsheet.
